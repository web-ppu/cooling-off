{
  "name": "cooling-off-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "쿨링오프 브라우저 확장 (소프트 인터셉트). web과 분리된 의존성.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "jsdom": "^25.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
